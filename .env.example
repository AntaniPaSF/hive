# ============================================================================
# Application Configuration
# ============================================================================

# Development/test environment: test, development, staging, production
ENV=test

# External port (for docker compose)
APP_PORT=8080

# Internal port (container internal port, default 8000)
# INTERNAL_PORT=8000

# ============================================================================
# Benchmark Suite Configuration
# ============================================================================

# LLM API endpoint URL (required for benchmark - no default)
# Example: BENCHMARK_API_URL=http://localhost:8080
BENCHMARK_API_URL=

# Benchmark suite: full (all tests) or smoke (quick subset)
BENCHMARK_SUITE=full

# Request timeout in seconds (default: 5.0)
BENCHMARK_TIMEOUT=5.0

# Fuzzy matching threshold for accuracy validation (default: 0.8)
# Range: 0.0 - 1.0, where 1.0 = exact match
BENCHMARK_THRESHOLD=0.8

# Ground truth dataset path (default: tests/benchmark/ground_truth.yaml)
BENCHMARK_GROUND_TRUTH=tests/benchmark/ground_truth.yaml

# ============================================================================
# CI/CD Configuration (GitHub Actions)
# ============================================================================

# Container Registry
REGISTRY=ghcr.io
REGISTRY_USERNAME=username
REGISTRY_PASSWORD=token

# Docker image configuration
IMAGE_NAME=hive-core

# GitHub Actions (these are auto-provided in CI, set locally if needed)
# GITHUB_TOKEN=token
# GITHUB_REPOSITORY=owner/repo

# ============================================================================
# Code Quality Thresholds
# ============================================================================

# Coverage threshold for tests (percentage)
COVERAGE_THRESHOLD=80

# Accuracy threshold for benchmarks (percentage)
ACCURACY_THRESHOLD=80
